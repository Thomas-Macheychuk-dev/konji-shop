# Included inside both HTTP and HTTPS server blocks only when
# LEGACY_SEO_REDIRECTS_ENABLED=true/1/on/yes.
if ($legacy_seo_product_redirect_target != "") {
    return 301 $legacy_seo_redirect_origin$legacy_seo_product_redirect_target;
}
